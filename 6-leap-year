public Boolean isLeapYear(Integer year) {
    if (Math.mod(year,400)==0
        ){
        return true;
    }
    else if (Math.mod(year,100)==0){
        return false;
    }
    else{
        return false;
    }
}
