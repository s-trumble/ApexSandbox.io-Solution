public Integer nextPrime(Integer num) {
    //code here
    if (num < 2){
        return 2;
    }
    Boolean isPrime = false;
    Integer nextPrime = num + 1;

    for(Integer i = 2; i = num - 1; i++){
        if(Math.mod(nextPrime,i)==0){
            isPrime = false;
        }
        return nextPrime;
    }
    return nextPrime;
}